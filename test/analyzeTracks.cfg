process Demo =  {
  #keep the logging output to a nice level
  include "FWCore/MessageLogger/data/MessageLogger.cfi"


source = PoolSource { untracked vstring fileNames = {"file:sim_rec_10muons_1-10GeV.root"} }


include "RecoVertex/PrimaryVertexProducer/data/TrackParameterAnalyzer.cfi"

module dump = EventContentAnalyzer {}
service = Tracer { untracked string indention = "$$"}
path p = {simpleTrackAnalysis & dump} 
} 
